<section id="about" class="bg-gray-800 py-16" style="background-color: #212529;">
  <div class="container mx-auto px-6 lg:px-12">
    <div class="relative flex justify-center text-center mb-12">
      <h2 class="text-5xl sm:text-6xl md:text-9xl uppercase font-semibold text-gray-400 opacity-10 w-full">
        About Me
      </h2>
      <p class="absolute self-center text-xl sm:text-2xl md:text-4xl font-semibold text-white leading-snug w-full">
        Know Me More
        <span class="block w-24 border-b-4 border-green-500 mx-auto mt-2"></span>
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-10">
      <div class="lg:col-span-7 xl:col-span-8 text-center lg:text-left">
        <h2 class="text-2xl md:text-3xl font-semibold text-white mb-4">
          I'm <span class="text-green-500">Prabin Nyaupane,</span> a Database Administrator
        </h2>
        <p class="text-gray-400 mb-4">
          I help organizations manage, optimize, and secure their databases at an enterprise level. Many teams and
          clients have achieved exceptional results while working with my expertise in SQL Server, performance tuning,
          and disaster recovery planning.
        </p>
        <p class="text-gray-400">
          Delivering solutions within time and budget which meet client's requirements is my motto. With strong hands-on
          experience in database administration, I ensure accuracy, reliability, and long-term stability in every
          project.
        </p>
      </div>

      <div class="lg:col-span-5 xl:col-span-4">
        <div class="lg:pl-6 text-center lg:text-left">
          <ul class="space-y-3 text-gray-200">
            <li><span class="font-semibold">Name:</span> Prabin Nyaupane</li>
            <li><span class="font-semibold">Email:</span> <a href="mailto:nyaupane.prabin8838@gmail.com"
                class="text-green-400 hover:underline">nyaupane.prabin8838@gmail.com</a></li>
            <li><span class="font-semibold">Age:</span> 27</li>
            <li><span class="font-semibold">From:</span> Kathmandu, Nepal</li>
          </ul>
        </div>
      </div>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-6 mt-12 text-center">
      <div class="counter-animation">
        <h4 class="text-4xl font-bold text-gray-400" data-target="4">0</h4>
        <p class="text-gray-300">Years Experience</p>
      </div>
      <div class="counter-animation">
        <h4 class="text-4xl font-bold text-gray-400" data-target="10">0</h4>
        <p class="text-gray-300">Projects Done</p>
      </div>
      <div class="counter-animation">
        <h4 class="text-4xl font-bold text-gray-400" data-target="20">0</h4>
        <p class="text-gray-300">Awards</p>
      </div>
    </div>
  </div>
</section>


<script>
  document.addEventListener('DOMContentLoaded', function () {
    const counters = document.querySelectorAll('h4[data-target]');
    let started = false; // to run only once

    function runCounters() {
      counters.forEach(counter => {
        const target = +counter.getAttribute('data-target');
        let count = 0;
        const increment = target / 100;
        const hasPlus = counter.textContent.includes('+');

        const update = () => {
          count += increment;
          if (count < target) {
            counter.innerText = Math.ceil(count) + (hasPlus ? '+' : '');
            requestAnimationFrame(update);
          } else {
            counter.innerText = target + (hasPlus ? '+' : '');
          }
        }
        update();
      });
    }

    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting && !started) {
          started = true;
          runCounters();
        }
      });
    }, { threshold: 0.5 });

    const statsSection = document.querySelector('#about');
    observer.observe(statsSection);
  });
</script>